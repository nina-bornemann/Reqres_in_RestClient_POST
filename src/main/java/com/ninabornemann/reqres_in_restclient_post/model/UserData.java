package com.ninabornemann.reqres_in_restclient_post.model;

public record UserData(String id,
                       String email,
                       String first_name,
                       String last_name,
                       String avatar) {
}
